# Terminal multiplexer
# https://github.com/tmux/tmux
# cSpell:words christoomey joshmedeski lazygit brightblack nobold gitmux catppuccin

set -g base-index 1
set -g detach-on-destroy off
set -g escape-time 0
set -s extended-keys on
set -g focus-events on
set -g history-limit 1000000
set -g mouse on
set -g renumber-windows on
set -s set-clipboard external
set -g status-interval 2
set -g status-left-length 200
set -g status-position top
set -g status-right ''
setw -g automatic-rename off
setw -g mode-keys vi
setw -g pane-base-index 1

set -g default-terminal 'tmux-256color'
set -g terminal-features 'xterm*:RGB:clipboard:ccolour:cstyle:focus:title,screen*:title,rxvt*:ignorefkeys,kitty*:RGB:clipboard,alacritty*:RGB:clipboard,tmux-256color:RGB:clipboard,screen-256color:RGB'
set -g terminal-overrides ''
set -g allow-passthrough on

set -g message-command-style 'bg=default,fg=yellow'
set -g message-style 'bg=default,fg=yellow'
set -g mode-style 'bg=default,fg=yellow'
set -g pane-active-border-style 'fg=magenta,bg=default'
set -g pane-border-style 'fg=brightblack,bg=default'
set -g status-style 'bg=default'
set -g window-status-separator ' '

if-shell 'command -v gitmux >/dev/null 2>&1' \
  "set -g status-left '#{?client_prefix,#[fg=black,bg=yellow,bold] PREFIX #[default] ,}#[fg=blue,bold]#S#[fg=white,nobold]#(gitmux -timeout 200ms -cfg $HOME/.config/tmux/gitmux.conf) '" \
  "set -g status-left '#{?client_prefix,#[fg=black,bg=yellow,bold] PREFIX #[default] ,}#[fg=blue,bold]#S '"

set -g window-status-current-format '#[fg=black,bg=brightcyan,bold] #I #{?#{automatic-rename},#{?#{&&:#{==:#{pane_current_command},node},#{&&:#{!=:#{pane_title},#{host}},#{!=:#{pane_title},#{host_short}}}},#{pane_title},#W},#W} #[default]'
set -g window-status-format '#[fg=brightblack] #I #[fg=gray]#{?#{automatic-rename},#{?#{&&:#{==:#{pane_current_command},node},#{&&:#{!=:#{pane_title},#{host}},#{!=:#{pane_title},#{host_short}}}},#{pane_title},#W},#W} #[default]'

bind '%' split-window -c '#{pane_current_path}' -h
bind '"' split-window -c '#{pane_current_path}'
bind c   new-window -c '#{pane_current_path}'
bind 0   select-window -t :=10
bind g   new-window -n 'git' lazygit
bind h   select-pane -L
bind j   select-pane -D
bind k   select-pane -U
bind l   select-pane -R
bind N   command-prompt -I '#{b:pane_current_path}' -p 'New session' 'new-session -Ad -s "%%" -c "#{pane_current_path}" \; switch-client -t "%%"'
bind r   command-prompt -I '#W' "rename-window '%%'"
bind R   command-prompt -I '#S' "rename-session '%%'"
bind V   run-shell '$HOME/.config/tmux/scripts/tmux-window-picker.sh'
bind W   run-shell '$HOME/.config/tmux/scripts/tmux-smart-close.sh'
bind-key '{' swap-window -t -1 \; previous-window
bind-key '}' swap-window -t +1 \; next-window
bind-key x kill-pane

bind-key -T copy-mode-vi 'C-\' select-pane -l
bind-key -T copy-mode-vi 'C-h' select-pane -L
bind-key -T copy-mode-vi 'C-j' select-pane -D
bind-key -T copy-mode-vi 'C-k' select-pane -U
bind-key -T copy-mode-vi 'C-l' select-pane -R
bind-key -T copy-mode-vi 'v'   send-keys -X begin-selection

set -g @fzf-url-fzf-options '-p 60%,30% --prompt="   " --border-label=" Open URL "'
set -g @fzf-url-history-limit '2000'
set -g @continuum-restore 'on'
set -g @resurrect-capture-pane-contents 'off'
set -g @tpm-update 'M-U'
set-environment -g TMUX_PLUGIN_MANAGER_PATH '$HOME/.config/tmux/plugins'

set -g @catppuccin_flavor 'macchiato'
set -g @catppuccin_window_text ' #W'
set -g @catppuccin_window_current_text ' #W'
set -g @plugin 'christoomey/vim-tmux-navigator'
set -g @plugin 'joshmedeski/tmux-fzf-url'
set -g @plugin 'sainnhe/tmux-fzf'
set -g @plugin 'joshmedeski/tmux-nerd-font-window-name'
set -g @plugin 'catppuccin/tmux#v2.3.0'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'
set -g @plugin 'tmux-plugins/tpm'

run '$HOME/.config/tmux/plugins/tpm/tpm'

# Keep only tmux session manager keys (remove Pi-specific U binding).
unbind -T prefix U
bind -T prefix K run-shell '$HOME/.config/tmux/scripts/tmux-sesh-picker.sh'
bind O run-shell '$HOME/.config/tmux/scripts/tmux-sesh-picker.sh'
bind P run-shell '$HOME/.config/tmux/scripts/tmux-window-picker.sh'
