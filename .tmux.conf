# Tmux loads ~/.config/tmux/tmux.conf directly on modern versions.
# Keep this file as a compatibility placeholder so ~/.tmux.conf and the XDG
# config are not both applying settings and plugin lists.
