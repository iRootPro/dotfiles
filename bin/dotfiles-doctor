#!/usr/bin/env bash
# dotfiles:summary=Run dotfiles health checks
# dotfiles:examples=dotfiles doctor
# dotfiles:aliases=dotfiles check

set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
exec "$ROOT/doctor.sh" "$@"
